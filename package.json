{
  "name": "toolnest",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:smoke": "SMOKE=1 playwright test tests/e2e/smoke.spec.ts",
    "fixtures": "node scripts/gen-fixtures.mjs",
    "deploy": "pnpm dlx wrangler pages deploy dist --project-name=toolnest --branch=main"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^25.7.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
